
import { TestConfig } from './types';

export const TIME_OPTIONS: number[] = [15, 30, 60, 120];
export const WORDS_OPTIONS: number[] = [10, 25, 50, 100];

export const DEFAULT_TEST_CONFIG: TestConfig = {
  time: 30,
  words: 50,
};
